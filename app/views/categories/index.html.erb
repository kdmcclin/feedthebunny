<div class="row">
  <h3>Select from one of these categories for category-specific prompts.</h3>
	<div class="large-3 columns">
		<% @categories1.each do |category| %>
			<li><%= link_to category.label, category_path(category)%></li>
		<% end %>
	</div>
	<div class="large-3 columns">
		<% @categories2.each do |category| %>
			<li><%= link_to category.label, category_path(category)%></li>
		<% end %>
	</div>
	<div class="large-3 columns">
		<% @categories3.each do |category| %>
			<li><%= link_to category.label, category_path(category)%></li>
		<% end %>
	</div>
	<div class="large-3 columns">
		<% @categories4.each do |category| %>
			<li><%= link_to category.label, category_path(category)%></li>
		<% end %>
	</div>
</div>